[package]
name = "kvstore"
version = "0.1.0"
edition = "2021"

# 错误处理统一用 anyhow（按任务分工文档的约定）
[dependencies]
anyhow = "1"

[[bin]]
name = "server"
path = "src/bin/server.rs"

[[bin]]
name = "client"
path = "src/bin/client.rs"

[profile.dev]
opt-level = 0
